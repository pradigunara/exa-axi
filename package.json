{
  "name": "exa-axi",
  "version": "0.1.0",
  "description": "AXI-compliant Exa search CLI — token-efficient TOON output for web search, fetch, and advanced search",
  "type": "module",
  "bin": {
    "exa-axi": "./dist/bin/exa-axi.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "bun run src/bin/exa-axi.ts",
    "prepublishOnly": "npm run build",
    "release": "npm run build && git add dist && git commit -m \"rebuild dist\" --allow-empty && npm version patch -m \"v%s\" && git push --follow-tags"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@toon-format/toon": "^2.3.0",
    "axi-sdk-js": "^0.1.6",
    "exa-js": "^2.13.0"
  }
}
